<UserControl x:Class="WpfSkinExample.Square"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfSkinExample"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Width="100" Height="100" Style="{StaticResource BorderStyling}">
        <ListBox Margin="10" SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}">
            <ListBoxItem Content="zero"/>
            <ListBoxItem Content="one"/>
            <ListBoxItem Content="two"/>
            <ListBoxItem Content="three"/>
            <ListBoxItem Content="four"/>
            <ListBoxItem Content="five"/>
        </ListBox>
    </Border>

</UserControl>
